@{
    ViewData["Title"] = "Login";
}

<h2>Login</h2>
<form asp-controller="Login" asp-action="GoogleLogin" method="post">
    <button type="submit" class="btn btn-primary">Sign in with Google</button>
</form>